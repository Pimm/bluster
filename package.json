{
  "private": true,
  "workspaces": [
    "core", "jest"
  ],
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "scripts": {
    "coverage": "npm run coverage --workspace core",
    "test": "npm test --workspaces"
  }
}
