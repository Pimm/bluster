{
  "private": true,
  "workspaces": [
    "core", "jest"
  ],
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "scripts": {
    "coverage": "npm run-script coverage --workspace core",
    "test": "npm test --workspaces"
  }
}
