{
  "private": true,
  "devDependencies": {
    "@babel/preset-env": "^7.12.1",
    "lerna": "^3.22.1",
    "rollup": "^2.33.0",
    "rollup-plugin-babel": "^4.4.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "coverage": "cd core && npm run-script coverage",
    "test": "lerna run test"
  }
}
